# Homework-7

1. Let $K = k(\alpha_1, \ldots, \alpha_n)$ be fields. Prove that any automorphism $\sigma$ of $K$ that is $k$-linear is completely determined by its values $\sigma(\alpha_i)$, $i \leq n$. Show that a $k$-linear (field) automorphism $\sigma$ (as above) of $K$ is identity of $K$ iff $\sigma(\alpha_i) = \alpha_i$ for all $i$.

2. Are $\mathbb{Q}(\sqrt{2})$ and $\mathbb{Q}(\sqrt{3})$ isomorphic as fields?

3. Let $\Phi_n(x) \in \mathbb{Q}[x]$ be the $n^{th}$ cyclotomic polynomial. Find $\Phi_n$ for low values of $n$.

4. Prove the following identities:
    (i) $\Phi_m(x) = \dfrac{x^m - 1}{\prod_{d \mid m,\, d < m} \Phi_d(x)}$
    
    (ii) $\Phi_p(x) = x^{p-1} + x^{p-2} + \cdots + x + 1$, $p$ a prime.
    
    $\Phi_{p^n}(x) = \Phi_p(x^{p^{n-1}})$
    
    (iii) For $n = p_1^{r_1} \cdots p_s^{r_s}$ (prime factorization),\\
    $\Phi_n(x) = \Phi_{p_1 \cdots p_s}\left(x^{p_1^{r_1-1} \cdots p_s^{r_s-1}}\right)$
    
    (iv) For $n$ odd, $\Phi_{2n}(x) = \Phi_n(-x)$.
    
    (v) For $p$ a prime not dividing $n$, $\Phi_{pn}(x) = \dfrac{\Phi_n(x^p)}{\Phi_n(x)}$.
    
    (vi) $\Phi_n(x) = \prod_{d \mid n} (x^{n/d} - 1)^{\mu(d)}$; $\mu$ is Möbius function.
    
    (vii) $\sum_{d \mid n} \mu(d) = \begin{cases} 1 & n=1 \\ 0 & n > 1 \end{cases}$