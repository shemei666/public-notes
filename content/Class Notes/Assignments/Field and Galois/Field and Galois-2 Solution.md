---
title: draft note
draft: true
---

2. Write down a polynomial in $F(\beta )$ which has root $\alpha$, consider it as a polynomial in $F(\alpha )$.
3. there is $\mathbb{F}_{p}$ inside a finite field of char p. Hence consider it as a vector space over $\mathbb{F}_{p}$.
4. The points will lie on the unit circle and will be roots of unity and hence pick the one with smallest angle.
5. AFC there is such an element convert its polynomial to one where $\alpha$ is a root.
6. Consider its basis.
7. On a finite field all elements of $F^{\times }$ has order dividing $|F^{\times }|$ hence $x^{n}-1$ is a polynomial with all roots as all elements of the field.
8. Irreducible polynomial of degree 3 over $\mathbb{F}_{3}$ is $x^{3}+2x+1$.
9. $\frac{\mathbb{F}_{p}[X]}{(X^{3}+2X+1)}$
10. $\mathbb{F}_{p}^{\times}$ is cyclic, only the even power elements are squares.
11. Use 10.
