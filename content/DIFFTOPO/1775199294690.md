---
publish: true
---

# Sard's Theorem Proof & Brouwer Fixed Point Theorem

## Proof (Continued)

> [!NOTE]- Proof
> Using Sard's theorem, pick $p \in \partial X$ that is regular for $g$. Then $p$ is also regular for $\partial g$ as $\partial g = \text{Identity}$.
>
> Hence $g^{-1}(p)$ is a manifold with boundary $g^{-1}(p) \cap \partial X$ of codimension $k-1$, i.e., of dimension $1$. It is compact since $g^{-1}(p)$ is closed in $X$. But $g^{-1}(p) \cap \partial X$ can only be $\{p\}$.

---

## Lemma

> [!INFO] Lemma
> Let $\psi: T_1 \oplus T_2 \to V$ be an onto linear map with kernel $K$. Then the restriction of $\pi_2: T_1 \oplus T_2 \to T_2$ to $K$ is onto $\iff \psi|_{T_1}$ is onto.

**Exercise.**

---

## Brouwer Fixed Point Theorem

> [!TIP] Brouwer Fixed Point Theorem
> Every continuous map from a closed unit ball to itself has a fixed point.

> [!NOTE]- Proof (Partial)
> Suppose $f: D^n \to D^n$ has no fixed point. Define $g: D^n \to S^{n-1}$ by sending $x$ to the intersection of the ray from $f(x)$ to $x$ with $S^{n-1}$.
> 
> On the boundary $(d \cdot x \in S^{n-1}$, the smooth map $g$ satisfies...
> 
> We prove smoothness: $g(x) = \ldots$ so $\|x - s\| = \ldots$
> 
> We can now construct the required contradiction, namely the discriminant $> 0$ which ensures smoothness of $g$.
